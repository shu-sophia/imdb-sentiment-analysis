[project]
name = "imdb-sentiment-analysis"
version = "0.1.0"
description = "IMDb sentiment analysis using NumPy only (Deep Learning from Scratch 2 style)"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.24.0",
    "tensorflow>=2.13.0",
]
